<Page
    x:Class="MagonoteToolkit.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:bh="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:resources="clr-namespace:MagonoteToolkit.Resources"
    xmlns:viewmodels="clr-namespace:MagonoteToolkit.ViewModels"
    mc:Ignorable="d">

    <Page.DataContext>
        <viewmodels:SettingsPageViewModel />
    </Page.DataContext>

    <Grid Margin="10,10,10,10" HorizontalAlignment="Center">
        <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock Style="{StaticResource SubtitleTextBlockStyle}" Text="{x:Static resources:Strings.ExcelFileInspection}" />
                <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                    <TextBlock
                        Margin="10,5,5,5"
                        VerticalAlignment="Center"
                        Text="{x:Static resources:Strings.SettingsFilePath}" />
                    <TextBox
                        Width="500"
                        Margin="5,5,10,5"
                        Text="{Binding ExcelFileInspectionSettingsFilePath}">
                        <bh:Interaction.Triggers>
                            <bh:EventTrigger EventName="PreviewDragOver">
                                <bh:InvokeCommandAction Command="{Binding FilePathPreviewDragOverCommand}" PassEventArgsToCommand="True" />
                            </bh:EventTrigger>
                            <bh:EventTrigger EventName="Drop">
                                <bh:InvokeCommandAction Command="{Binding ExcelFileInspectionSettingsFilePathDropCommand}" PassEventArgsToCommand="True" />
                            </bh:EventTrigger>
                        </bh:Interaction.Triggers>
                    </TextBox>
                </StackPanel>

                <TextBlock Style="{StaticResource SubtitleTextBlockStyle}" Text="{x:Static resources:Strings.ExcelFileNumberToName}" />
                <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                    <TextBlock
                        Margin="10,5,5,5"
                        VerticalAlignment="Center"
                        Text="{x:Static resources:Strings.SettingsFilePath}" />
                    <TextBox
                        Width="500"
                        Margin="5,5,10,5"
                        Text="{Binding ExcelFileNumberToNameSettingsFilePath}">
                        <bh:Interaction.Triggers>
                            <bh:EventTrigger EventName="PreviewDragOver">
                                <bh:InvokeCommandAction Command="{Binding FilePathPreviewDragOverCommand}" PassEventArgsToCommand="True" />
                            </bh:EventTrigger>
                            <bh:EventTrigger EventName="Drop">
                                <bh:InvokeCommandAction Command="{Binding ExcelFileNumberToNameSettingsFilePathDropCommand}" PassEventArgsToCommand="True" />
                            </bh:EventTrigger>
                        </bh:Interaction.Triggers>
                    </TextBox>
                </StackPanel>
                <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                    <TextBlock
                        Margin="10,5,5,5"
                        VerticalAlignment="Center"
                        Text="{x:Static resources:Strings.ConvertRulesFilePath}" />
                    <TextBox
                        Width="500"
                        Margin="5,5,10,5"
                        Text="{Binding ExcelFileNumberToNameConvertRulesFilePath}">
                        <bh:Interaction.Triggers>
                            <bh:EventTrigger EventName="PreviewDragOver">
                                <bh:InvokeCommandAction Command="{Binding FilePathPreviewDragOverCommand}" PassEventArgsToCommand="True" />
                            </bh:EventTrigger>
                            <bh:EventTrigger EventName="Drop">
                                <bh:InvokeCommandAction Command="{Binding ExcelFileNumberToNameConvertRulesFilePathDropCommand}" PassEventArgsToCommand="True" />
                            </bh:EventTrigger>
                        </bh:Interaction.Triggers>
                    </TextBox>
                </StackPanel>

                <Button
                    Margin="10,5,10,5"
                    HorizontalAlignment="Stretch"
                    Command="{Binding SaveSettingsCommand}"
                    Content="{x:Static resources:Strings.Save}" />
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
